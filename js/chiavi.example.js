// Esempio di chiavi per API — NON inserire chiavi vere qui.
// Copia questo file in `chiavi.js` e sostituisci i placeholder con le tue chiavi.

const CHIAVI = {
  OPENAI_API_KEY: "inserisci_la_tua_openai_api_key_qua",
  ALTRO_SERVIZIO_KEY: "inserisci_altra_chiave_qua"
};

if (typeof module !== 'undefined') module.exports = CHIAVI;
